{
	"name": "browser-extension-direct-link-extractor",
	"description": "Browser Extension - Extracts and generates direct download links for fuckingfast.co and datanodes.to.",
	"version": "1.0.2",
	"type": "module",
	"scripts": {
		"clean": "shx rm -rf dist build",
		"copy-assets": "shx cp -r src/* dist/",
		"prebuild": "npm rum clean && shx mkdir -p dist",
		"build": "npm run copy-assets"
	},
	"devDependencies": {
		"@types/chrome": "^0.1.40",
		"@types/firefox-webext-browser": "^143.0.0",
		"@types/node": "^25.6.0",
		"shx": "^0.4.0"
	}
}